"""
Core infrastructure for the autonomous QA system.

Modules:
    agent_runner   — Generic agent execution loop
    mcp_client     — Multi-MCP server connection manager
    blackboard     — Append-only shared conversation log
    registry       — Agent manifest scanner and validator
    session        — Session state and evidence directory management
    site_model     — Site graph data structure
    tier1_locator  — Deterministic keyword-to-element-ref matching
"""
